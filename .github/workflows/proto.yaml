name: Test

on: [push]

jobs:
  lint:
    runs-on: docker://golang:1.13.3-alpine
    steps:
      - name: Lint
        run: |
              echo Add other actions to build,
              echo test, and deploy your project.
